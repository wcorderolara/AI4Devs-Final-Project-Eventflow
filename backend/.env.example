# EventFlow backend — variables de entorno (ejemplo).
# NUNCA commitear un DATABASE_URL real ni secretos (SEC-01, SEC-02).
# Valor dummy suficiente para `prisma validate` / `prisma generate` (US-099 no conecta a BD real).
DATABASE_URL="postgresql://user:password@localhost:5432/eventflow?schema=public"
