// Stub representativo de naming convention (US-090 / BE-006). Doc 14 §24.2: `<feature>.routes.ts`.
// Router vacío; las rutas reales se montan en la feature story de identity-access.
import { Router } from 'express';

export const identityAccessRouter = Router();
