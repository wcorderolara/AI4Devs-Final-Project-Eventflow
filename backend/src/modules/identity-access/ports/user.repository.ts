// Stub representativo de naming convention (US-090 / BE-006). Doc 14 §24.2: `<entity>.repository.ts`.
// Interface (puerto) vacía intencionalmente; los métodos se definen en la feature story de
// identity-access. La interface vacía es el placeholder prescrito por la Tech Spec de US-090.
// eslint-disable-next-line @typescript-eslint/no-empty-object-type -- stub de puerto US-090; se completa en la feature story
export interface UserRepository {}
