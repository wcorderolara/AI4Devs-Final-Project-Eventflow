// Placeholder de capa (US-090 estructural). `event-catalog` accede a Prisma directamente
// desde `application/*.use-case.ts` (patrón paridad US-075 service-catalog) — sin adapters
// intermedios. `export {}` mantiene el módulo compilable con `tsc`.
export {};
