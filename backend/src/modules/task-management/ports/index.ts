// Placeholder de capa (US-090). Sin lógica de feature; la implementación pertenece a la
// feature story de este bounded context. `export {}` mantiene el módulo compilable con `tsc`.
// US-034 (PB-P2-004 / BE-003): el puerto `T7CandidateSourcePort` vive en
// `src/shared/application/` para preservar boundaries; el adapter Prisma vive en
// `task-management/infrastructure/prisma-event-task-t7.repository.ts`.
export {};
