import { describe, expect, it } from 'vitest';

// Smoke unit placeholder: mantiene `npm run test` verde hasta que lleguen suites reales.
describe('smoke', () => {
  it('confirma que el runner de tests ejecuta', () => {
    expect(1 + 1).toBe(2);
  });
});
