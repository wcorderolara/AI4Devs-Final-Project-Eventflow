import { CreateEventPage } from '@/features/events';

// US-009 — asistente de creación de evento. El gating de sesión/rol lo aplica el layout `(app)`.
export default function OrganizerCreateEventPage() {
  return <CreateEventPage />;
}
