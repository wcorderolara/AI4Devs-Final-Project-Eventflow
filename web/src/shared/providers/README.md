# `shared/providers/`

Providers globales de la app (Client Components envolviendo el árbol).

Vacía en US-103. `<QueryProvider>` (TanStack Query) y `<MSWProvider>` llegan en **US-106**;
`<IntlProvider>` en **US-104**; `<ThemeProvider>` / navegación por rol en **US-107**.
