Use the `eventflow-user-story-delivery-workflow` skill.

Execute the complete EventFlow User Story delivery workflow for:

```text
$ARGUMENTS
```

The argument must be the path to exactly one EventFlow User Story Markdown file.

Follow the exact validation, orchestration, state management, stop conditions, resume behavior, artifact discovery rules, and output format defined by the skill.

Do not reimplement the workflow inside this command.

Return the result in neutral Spanish LATAM.
